# Only used during `npm run dev` (Vite dev server).
# In production builds this file is absent, so VITE_API_BASE
# falls back to "" → relative URLs (same-origin with FastAPI).
VITE_API_BASE=http://127.0.0.1:8000
